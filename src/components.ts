import { Types, defineComponent } from "bitecs";

export const TemplateComponent = defineComponent({
  color: Types.ui32,
});

export const NetworkedTemplateComponent = defineComponent({
  color: Types.ui32,
});
